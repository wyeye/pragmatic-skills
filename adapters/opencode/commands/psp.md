---
description: Enter the repository's Pragmatic Skills Pack workflow.
---

Use the Pragmatic Skills Pack entry workflow. Prefer a project-local `skills/using-pragmatic-skills/SKILL.md` only when it exists; otherwise use the host-installed or global PSP runtime skills. Route the user's ordinary task through its direct-route or triage contract, select one primary mode, and attach real evidence to completion claims. If the PSP runtime is unavailable, state that structured tracing is not enabled.
