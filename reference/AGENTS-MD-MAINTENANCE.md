# AGENTS.md maintenance

Project guidance and the PSP managed block have different owners. The project may freely edit content outside the block. The installer alone updates the block between `<!-- PSP:BEGIN -->` and `<!-- PSP:END -->`.

A malformed or duplicated marker pair is a conflict. The installer never guesses which region is authoritative.
